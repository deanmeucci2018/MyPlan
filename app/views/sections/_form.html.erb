<%= form_with(model: section, local: true) do |form| %>
  <% if section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(section.errors.count, "error") %> prohibited this section from being saved:</h2>

      <ul>
      <% section.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :start_time %>
    <%= form.time_select :start_time, id: :section_start_time %>
  </div>

  <div class="field">
    <%= form.label :end_time %>
    <%= form.time_select :end_time, id: :section_end_time %>
  </div>

  <div class="field">
    <%= form.label :professor %>
    <%= form.text_field :professor, id: :section_professor %>
  </div>

  <div class="field">
    <%= form.label :section_letter %>
    <%= form.text_field :section_letter, id: :section_section_letter %>
  </div>

  <div class="field">
    <%= form.label :semester %>
    <%= form.text_field :semester, id: :section_semester %>
  </div>

  <div class="field">
    <%= form.label :section_year %>
    <%= form.number_field :section_year, id: :section_section_year %>
  </div>

  <div class="field">
    <%= form.label :section_days %>
    <%= form.text_field :section_days, id: :section_section_days %>
  </div>

  <div class="field">
    <%= form.label :course_name %>
    <%= form.select :course_id, @options %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
